springBoot {
    buildInfo {
        properties {
            version = System.getProperty('ARTEFACT_VERSION') ?: project.version
        }
    }
}

tasks.named('bootBuildInfo') {
    outputs.upToDateWhen { false }
}